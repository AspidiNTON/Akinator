#ifndef AKINATOR_H
#define AKINATOR_H

#include "tree.h"
#include "dump.h"

bool gameLoop(Node* node);

bool playGame(Node* node);

bool showTree();

bool printElementDefinition(Node* node);

#endif